node_modules/
dist/
*.js.map
*.d.ts
!bin/*.js
.lazy-claudecode/
.env
.DS_Store
*.tgz
coverage/

# Component dist (auto-generated)
# NOTE: MCP server dist/ bundles are committed because they're pre-built
# from the upstream monorepo and can't be rebuilt locally.
# src/*/dist/ — NOT ignored, these are tracked
